# Cordis row that mounts this plugin once it is packaged as an npm package and
# installed into the profile's node_modules (current activation is dynamic:
# cordis_define + cordis_run — see README.md).
# Exposes three tools: visual_capture, design_tokens_parser, a11y_validator.
- id: visual-capture
  name: '@garder500/harness-plugin-visual-capture'
